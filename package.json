{
  "name": "nuxt-prisma-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "prisma generate && prisma db push && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "prisma generate && nuxt prepare",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "better-auth": "^1.2.5",
    "nuxt": "^3.16.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}
